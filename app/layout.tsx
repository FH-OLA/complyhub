import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: 'ComplyHub — Compliance monitoring for UK small businesses',
  description:
    'ComplyHub helps UK small businesses stay compliant by tracking filing deadlines, generating compliance policies, and providing a real-time compliance score.',
}

// viewport-fit=cover enables env(safe-area-inset-*) CSS functions used by
// the mobile navigation drawer and the FeedbackWidget fixed button to clear
// the iPhone home indicator and Dynamic Island.
export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
